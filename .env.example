# Optional: Google OAuth Client ID for "Sign in with Google" flow.
# If not set, users will use API keys instead (no OAuth button shown).
# Create at: https://console.cloud.google.com/apis/credentials
# Type: OAuth 2.0 Client ID (Web application)
# Add http://localhost:5174 as an Authorized JavaScript Origin.
VITE_GOOGLE_CLIENT_ID=
